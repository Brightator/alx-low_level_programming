#include "main.h"
/**
 * _isupper - uppercase letters
 *
 * @c: character to check
 *
 * Return: 0 or 1
 */

/* checks for uppercase */
int _isupper(int c)
{
	/* conditions for uppercase */
	if (c == _isupper(c))
		return (1);
	else
		return (0);
}
