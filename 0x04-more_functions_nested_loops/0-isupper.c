#include "main.h"
/**
 * main - _isupper.
 *
 * Return: either 1 or 0.
 */

/* checks for uppercase */
int _isupper(int c)
{
	/* conditions for uppercase */
	if (c == _isupper(c))
		return (1);
	else
		return (0);
}
